// BrokenDLL.cpp
#include <windows.h>

// This exported function is intentionally broken.
// When called, it will dereference a null pointer, causing a crash.
extern "C" __declspec(dllexport) void BrokenFunction() {
    int* ptr = nullptr;
    *ptr = 0; // Intentional crash
}

// DllMain: The entry point for the DLL.
BOOL APIENTRY DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved) {
    switch (ul_reason_for_call) {
    case DLL_PROCESS_ATTACH:
        // Initialization code can be placed here if needed.
        break;
    case DLL_THREAD_ATTACH:
    case DLL_THREAD_DETACH:
    case DLL_PROCESS_DETACH:
        break;
    }
    return TRUE;
}
 // BrokenDLL.cpp
#include <windows.h>

// This exported function is intentionally broken.
// When called, it will dereference a null pointer, causing a crash.
extern "C" __declspec(dllexport) void BrokenFunction() {
    int* ptr = nullptr;
    *ptr = 0; // Intentional crash
}

// DllMain: The entry point for the DLL.
BOOL APIENTRY DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved) {
    switch (ul_reason_for_call) {
    case DLL_PROCESS_ATTACH:
        // Initialization code can be placed here if needed.
        break;
    case DLL_THREAD_ATTACH:
    case DLL_THREAD_DETACH:
    case DLL_PROCESS_DETACH:
        break;
    }
    return TRUE;
}
 // BrokenDLL.cpp
#include <windows.h>

// This exported function is intentionally broken.
// When called, it will dereference a null pointer, causing a crash.
extern "C" __declspec(dllexport) void BrokenFunction() {
    int* ptr = nullptr;
    *ptr = 0; // Intentional crash
}

// DllMain: The entry point for the DLL.
BOOL APIENTRY DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved) {
    switch (ul_reason_for_call) {
    case DLL_PROCESS_ATTACH:
        // Initialization code can be placed here if needed.
        break;
    case DLL_THREAD_ATTACH:
    case DLL_THREAD_DETACH:
    case DLL_PROCESS_DETACH:
        break;
    }
    return TRUE;
}
 // BrokenDLL.cpp
#include <windows.h>

// This exported function is intentionally broken.
// When called, it will dereference a null pointer, causing a crash.
extern "C" __declspec(dllexport) void BrokenFunction() {
    int* ptr = nullptr;
    *ptr = 0; // Intentional crash
}

// DllMain: The entry point for the DLL.
BOOL APIENTRY DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved) {
    switch (ul_reason_for_call) {
    case DLL_PROCESS_ATTACH:
        // Initialization code can be placed here if needed.
        break;
    case DLL_THREAD_ATTACH:
    case DLL_THREAD_DETACH:
    case DLL_PROCESS_DETACH:
        break;
    }
    return TRUE;
}
 